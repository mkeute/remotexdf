from setuptools import setup
 
setup(
   name='remotexdf',    # This is the name of your PyPI-package.
   packages = ['remotexdf']                  # The name of your scipt, and also the command you'll be using for calling it
)
